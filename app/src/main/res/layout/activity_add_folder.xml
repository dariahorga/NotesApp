<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".AddFolderActivity">

    <TextView
        android:id="@+id/addFolderHeading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="26dp"
        android:layout_marginLeft="26dp"
        android:padding="2dp"
        android:text="Add Folder"
        android:textColor="@color/purple"
        android:textSize="24sp" />

    <ImageView
        android:id="@+id/addFolderButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_done_24"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@id/addFolderHeading"
        android:layout_marginEnd="15dp"/>

    <EditText
        android:id="@+id/folderNameEditText"
        android:layout_width="379dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/addFolderHeading"
        android:layout_marginTop="12dp"
        android:background="@drawable/purple_border"
        android:hint="Enter folder name"
        android:inputType="text"
        android:maxLines="1"
        android:padding="20dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"/>

</RelativeLayout>
